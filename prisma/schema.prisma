generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model Restaurant {
  id        String   @default(cuid()) @id
  registeredAt DateTime @default(now())
  name      String
}
